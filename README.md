# RV-Sparse-Coding-Challenge
Coding challenge submission for the Micro Electronics Research Lab - UIT (MERL-UIT)

The code includes an automated test harness. To run this implementation, execute the following:

```bash
gcc -o run challenge.c -lm
./run
```
